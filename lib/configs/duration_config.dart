const kDotAnimationDuration = Duration(milliseconds: 200);
const kWidgetAnimationDuration = Duration(milliseconds: 300);
const kRouteAnimationDuration = Duration(milliseconds: 250);
const kSplashDuration = Duration(seconds: 2);
const kContainerCurrentImageDuration = Duration(milliseconds: 250);
const kDefaultDuration = Duration(milliseconds: 250);
